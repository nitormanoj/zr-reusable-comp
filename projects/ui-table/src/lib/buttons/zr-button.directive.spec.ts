import { ZrButtonDirective } from './zr-button.directive';

describe('ZrButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ZrButtonDirective();
    expect(directive).toBeTruthy();
  });
});
